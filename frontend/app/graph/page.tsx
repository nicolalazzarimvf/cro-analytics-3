import GraphClient from "./GraphClient";

export default function GraphPage() {
  return (
    <main className="mx-auto max-w-[96rem] px-6 py-10 h-[calc(100dvh-64px)]">
      <GraphClient />
    </main>
  );
}
