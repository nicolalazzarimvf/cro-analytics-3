{
  "name": "cro-analytics-3-root",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "vercel-build": "cd frontend && npm ci --include=dev && npm run build"
  },
  "dependencies": {
    "next": "15.5.9"
  }
}
